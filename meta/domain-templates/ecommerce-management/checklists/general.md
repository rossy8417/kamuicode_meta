# E-commerce Management Checklist (MUST/SHOULD)

## MUST
- All outputs under `${PROJECT_DIR}`
- Artifacts for sharing
- No local path in `uses:`
- `products.json` and assets exist before publishing

## SHOULD
- Export feeds to `${PROJECT_DIR}/final/`
