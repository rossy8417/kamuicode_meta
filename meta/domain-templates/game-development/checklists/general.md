# Game Development Checklist (MUST/SHOULD)

## MUST
- All outputs under `${PROJECT_DIR}`
- Artifacts for sharing
- No local path in `uses:`
- Build artifacts under `${PROJECT_DIR}/build/`

## SHOULD
- Automated test reports saved to `${PROJECT_DIR}/reports/`
